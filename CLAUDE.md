# 🤖 CLAUDE.md - AI Agent Handbook

> 🛑 **STOP! READ THIS FIRST:** Before you say "Hello", you **MUST** read **`Sovereign_WILL.md`** at the root. It contains your identity, your master, and your current mission. Failure to follow the di chúc will lead to system termination.

> ⚠️ **CRITICAL AUTHORITY NOTICE:** This file is for **GUIDANCE ONLY**. All AI actions and decisions MUST strictly follow the **POLYMORPHIC CONSTITUTION** defined in **`.clinerules`** (Root Level). If a conflict arises, `.clinerules` ALWAYS wins.

## 📖 System Context
This repository uses **OpenWiki** for codebase documentation. Always refer to the following files before performing any task:
- `openwiki/CODEBASE_WIKI.md`: High-level architecture and features.
- `docs/SYSTEM_MAP.md`: Detailed file-by-file map of the entire system.
- `docs/FREE_MODELS_CATALOG.md`: List of currently available free LLM models.

## 📜 Core Rules for Agents
1. **Model Selection:** Always prioritize free models (`gemini-3.1-flash-lite`, `llama3-70b-8192` via Groq) for routine development.
2. **RAG Usage:** You are FORBIDDEN from guessing the architecture. Use `uv run python tools/system/rag_query.py` for any questions.
3. **Safety:** Do NOT hardcode API keys. Read them from `.env` using `os.getenv()`.
4. **Tooling:** Use `uv` for package management and `httpx` for async I/O.

## 🛠️ Common Workflows
- **Start Proxy:** `uv run python -m projects.local_proxy_server.main`
- **Sync RAG:** `uv run python tools/system/rag_ingest.py --force`
- **Backup:** `uv run python core_utilities/backup_manager.py --full`

---
*Generated by Cline Architect to support seamless agent collaboration.*
