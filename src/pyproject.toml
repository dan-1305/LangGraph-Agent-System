[project]
name = "src"
version = "1.0.0"
description = "Core AI Factory and utilities"
requires-python = ">=3.10"
dependencies = [
    # Inherits from workspace or list specific ones
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
