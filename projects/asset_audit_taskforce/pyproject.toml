[project]
name = "disk_cleaner"
version = "0.1.0"
description = "Disk Cleaner & Deep Scanner utilities"
requires-python = ">=3.9"
dependencies = [
    "streamlit>=1.30.0",
    "pandas>=2.0.0",
    "psutil>=5.9.0",
]
